# smart-fire 作業ルール（Codeで触る前に、毎回最初に読むこと）

このルールは、過去に起きた3つの事故（①古いローカルからの巻き戻し、②別プロジェクトのプレビュー誤表示、③承諾ダイアログへの絶対パス露出）を防ぐためのものです。smart-fire関連（smart-fire-site / smart-fire-type-diagnosis）をCodeで編集する前に、必ずこの手順から始めてください。

## 0. 触ってよいもの／絶対に触らないもの
- 触ってよい：smart-fire-site、smart-fire-type-diagnosis。
- **絶対に触らない：smart-fire-lp（書籍の作品ページ）、smart-fire-reader-site（旧・バックアップ系）、その他 smart-fire 名のリポジトリ。**
- 迷ったら、編集・pushせずに停止して確認する。

## 1. 作業開始時：必ず「ライブ＝ローカル」を作ってから編集する（巻き戻し防止）
1. 対象リポジトリの**実際のGitHub Pages公開元**をAPIで確認する（`gh api repos/aoi-kurochan/<repo>/pages` の source.branch / source.path）。READMEは信用しない。
   - 参考：smart-fire-site は gh-pages / ルート。smart-fire-type-diagnosis は main。ただし毎回APIで実値確認する。
2. ライブHTMLを取得し、その内容と**バイト単位で一致するブランチ/コミット**をリモートから特定する（`git fetch origin --all --prune` 後に確認）。
3. 一致するブランチをチェックアウトして、ローカル作業ツリー＝ライブにする。編集はこの状態からのみ始める。
4. **ライブと一致するブランチがリモートに見つからない場合は、編集・pushせず停止して報告する。** 古いローカルからのpushは、公開ページを巻き戻す破壊的操作なので絶対にしない。

## 2. プレビュー確認：必ずそのプロジェクト専用サーバーで見る（誤表示防止）
- 既存のプレビューサーバー（他プロジェクトが立てたもの）を**再利用しない**。
- 対象プロジェクトの作業ディレクトリで、専用ポートの静的サーバーを毎回立て直し、そのURLで確認する。
- プレビューに別サイトが出たら、確認先を疑い、まず正しいサーバーに向け直す（ファイルは触らない）。

## 3. 個人情報・機密の露出防止（報告・コミット・画面すべて）
- **コミット・push・報告文に、次を絶対に含めない：** ローカル絶対パス（/Users/… など）、マシンのユーザー名、メールアドレス、MyASPの内部ID・フォームの生URL以外の内部情報、APIトークン。
- パスを示す必要があるときは、リポジトリ名＋相対パスだけで表現する（例：`smart-fire-site/index.html`）。絶対パスは書かない。
- shell実行やダイアログに絶対パスが出そうな場合は、`cd` を相対で扱う・出力を絞るなどして、フルパスを表に出さない工夫をする。
- push前に、変更差分（コミット対象）に対して個人情報スキャンを行い、ヒット0を報告する。

## 4. デプロイと最終確認（毎回）
- コミット著者は必ず `aoi-kurochan <aoi-kurochan@users.noreply.github.com>`。
- 通常pushのみ。force push・ブランチ削除・履歴書き換え・リネームは禁止。
- push後、キャッシュバスター付きでライブを取得し、ライブ＝ローカル一致を確認。意図した変更だけが入り、デザインの巻き戻りが無いことを報告する。
- 最後に、公開されたライブHTMLに対して、絶対パス・ユーザー名・メール・トークンが0件であることを確認して報告する（本番の最終クリーンチェック）。

## 5. 迷ったときの原則
- 破壊的になりうる操作、対象ファイルの取り違えの疑い、指示の「現行テキスト」が実在しない——このいずれかを感じたら、**編集・pushせず、状況を報告して指示を待つ。** 推測で進めない。
